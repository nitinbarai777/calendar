<br /><br />
		<ul id="nav_sub">
            <% menus.each do |menu| %>
               <li><span>
                  <%= menu.name %></span>
                  <ul>
                  <% menu.pages.each do |pg| %>
                     <li><%= link_to pg.title, pg %></li>
                  <% end %>
                  </ul>
                  <div class="clear"></div>
               </li>
            <% end %>
		</ul>


